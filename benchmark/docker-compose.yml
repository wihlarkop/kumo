services:
  kumo:
    build:
      context: ..
      dockerfile: benchmark/kumo/Dockerfile
      args:
        KUMO_VERSION: ${KUMO_VERSION:-unknown}
    environment:
      - TARGET_URL=${TARGET_URL:-https://books.toscrape.com/catalogue/page-1.html}
      - TARGET_URLS=${TARGET_URLS:-}
      - SCALE_MODE=${SCALE_MODE:-false}
      - CONCURRENCY=${CONCURRENCY:-16}
    volumes:
      - ./results:/results
    networks:
      - bench

  scrapy:
    build:
      context: ./scrapy
    environment:
      - TARGET_URL=${TARGET_URL:-https://books.toscrape.com/catalogue/page-1.html}
      - CONCURRENCY=${CONCURRENCY:-16}
    volumes:
      - ./results:/results
    networks:
      - bench

  colly:
    build:
      context: ./colly
    environment:
      - TARGET_URL=${TARGET_URL:-https://books.toscrape.com/catalogue/page-1.html}
      - CONCURRENCY=${CONCURRENCY:-16}
    volumes:
      - ./results:/results
    networks:
      - bench

  mockserver:
    build:
      context: ./mockserver
    networks:
      - bench

networks:
  bench:
